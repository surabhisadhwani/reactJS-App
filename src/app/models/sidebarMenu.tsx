import React from "react";
import { Dashboard } from "@material-ui/icons";

const SidebarMenu = [
  {
    name: "Dashboard",
    icon: <Dashboard />,
    url: "/page/dashboard",
  },
];

export default SidebarMenu;
